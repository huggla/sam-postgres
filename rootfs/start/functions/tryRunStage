tryRunStage(){
   local -
   if [ -d "/start/stage3" ]
   then
      set +f
      for file in /start/stage3/*
      do
         if [ -f "$file" ]
         then
            . "$file"
         fi
      done
   fi
}
