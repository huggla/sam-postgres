# Set in stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   sed -i "s/:SRSNO/$VAR_SRID/I" /initdb/045.template_3dcitydb.sql
   sed -i "s/:'GMLSRSNAME'/$VAR_SRSNAME/I" /initdb/045.template_3dcitydb.sql
fi
